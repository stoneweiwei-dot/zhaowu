# White-screen production fix verification

Validates the pure Vite SPA build after removing the TanStack Start document/SSR mismatch. Expected deploy artifact is dist/ with transformed JS/CSS assets.
