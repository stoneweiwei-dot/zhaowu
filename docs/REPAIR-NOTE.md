# Runnable main repair

This branch only restores missing application shell code and build dependencies. It does not change locked Bazi, calendar, palm, method-routing, or past-life short-circuit algorithms.

Build must pass before merge to `main`.
