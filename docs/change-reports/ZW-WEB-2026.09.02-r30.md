# 昭梧更新報告｜ZW-WEB-2026.09.02-r30

## 本次改動

移除home-quiz-paper.css內強制表單使用不透明米色的單一舊規則。表單現在與其餘區塊共用r29半透明宣紙樣式，背景山水可以透出；仍保留淺色輸入欄與深色字。

## 為什麼改

r29正式畫面驗收的computed style顯示表單仍為rgb(251,245,233)，與參考圖要求的半透明效果不符。原因是舊選擇器更具體，蓋過最終外觀設定。直接移除相衝底色，不再疊加更高權重。

## 影響範圍

僅提問表單背景底色。r29的山水底、朱紅按鈕、站主新上傳梧桐影片及並蒂蓮靜圖不變。四柱計算、登入權限、支付、資料庫、報告歷史、單張隨機吉祥圖及文章折疊不變。

## 回滾

還原本次單一樣式刪除即可恢復r29不透明表單。無資料遷移，無後端發布。

## 驗證

更新回歸契約，禁止舊不透明背景再次遮住山水。347/347完整測試PASS、TypeScript PASS、Vite build PASS（3.09s）。正式computed style／畫面驗證記入release_history。r29已驗證朱紅按鈕、背景素材、登入頁無格線；r30驗證表單透明度。iPhone Safari與真實帳戶重新登入仍未實機驗证，不宣稱完全驗收。
