# 昭梧更新報告｜ZW-WEB-2026.09.24-r192

## 本次改動

- 登入影片單次最多播放 15 秒，然後停在靜態封面；登入流程不重播的 r191 契約維持。
- 聲音控制改成單一喇叭圖示，觸控區為 44px，保留可讀的無障礙名稱。
- 後台登入素材清單只顯示影片；現有上傳流程將影片時長上限由 5 秒調為 15 秒。

## 為什麼改

登入影片的播放時長、聲音控制與後台素材需要符合站主指定的簡潔操作。這一版不會把大型原片轉碼或暫停的 Storage 上傳誤稱為可用功能。

## 影響範圍

`/login` 影片呈現、站主登入影片管理器、公開更新紀錄。

## 受保護範圍

不改站主身份驗證、付款、命理計算、報告、Supabase schema、既有媒體原件及 Storage 寫入凍結。大檔來源（200–500 MB）的續傳、轉碼與發佈尚未實作。

## 驗證狀態

合併前檢查 Engine suite、Deploy gate、iPhone Safari、TypeScript 和 Vite build；合併後比對 Production SHA，檢查 `/`、`/login`、`/updates`。真 iPhone 互動與站主登入後畫面須另以正式站實測；CI／READY 不等於完成驗收。

## 回滾

回滾本次前端與管理器程式到 r191；沒有資料庫 migration 或 Storage 物件變更。
