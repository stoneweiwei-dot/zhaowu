import assert from"node:assert/strict";import{readFile}from"node:fs/promises";import test from"node:test";const stats=await readFile(new URL("../src/lib/site-stats.ts",import.meta.url),"utf8");const shell=await readFile(new URL("../src/components/site-shell.tsx",import.meta.url),"utf8");const report=await readFile(new URL("../docs/change-reports/ZW-WEB-2026.09.05-r50.md",import.meta.url),"utf8");const agents=await readFile(new URL("../AGENTS.md",import.meta.url),"utf8");test("public footer always exposes current release and cumulative update count",()=>{assert.match(stats,/ZW-WEB-2026\.09\.05-r50/);assert.match(stats,/updateNumber:\s*50/);assert.match(shell,/data-site-release/);assert.match(shell,/累計更新/);assert.match(shell,/data-latest-change-report/)});test("every production backend change requires a matching change report",()=>{assert.match(report,/# ZW-WEB-2026\.09\.05-r50/);assert.match(report,/## What changed/);assert.match(report,/## Why/);assert.match(report,/## Affected scope/);assert.match(report,/## Rollback/);assert.match(agents,/MANDATORY RELEASE LEDGER/);assert.match(agents,/docs\/change-reports/);assert.match(agents,/release_history/);assert.match(agents,/CANONICAL METAPHYSICS DEFAULT/)})