name: netlify-production-deploy

# DISABLED 2026-08-21
# Netlify production is no longer the deployment target.
# Official production is the existing Vercel project:
# https://stone-zhaowu-official.vercel.app/
# Do not re-enable this workflow without an explicit new instruction.

on:
  workflow_dispatch:
    # Manual only, and the job is forced to skip.

permissions:
  contents: read
  issues: write

jobs:
  deploy:
    if: false
    runs-on: ubuntu-latest
    timeout-minutes: 1
    steps:
      - name: Netlify production deploy disabled
        run: |
          echo "Netlify production workflow is permanently disabled."
          echo "Use the existing Vercel project stone-zhaowu-official instead."
          exit 1
