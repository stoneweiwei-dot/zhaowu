/** STONE painted auspicious motifs for sitewide scatter. */
import { PAINTED_PART as A } from "@/lib/painted-motifs-a";
import { PAINTED_PART as B } from "@/lib/painted-motifs-b";

export const PAINTED_MOTIFS: string[] = [...A, ...B];
export const PAINTED_COUNT = PAINTED_MOTIFS.length;
